# Demo data

Run `screenshield demo` to generate a synthetic support-console screenshot,
explicit OCR/visual ground truth, a safe copy, and a privacy-preserving JSON report.
Install the `demo` extra to render a standards-compliant QR code.
